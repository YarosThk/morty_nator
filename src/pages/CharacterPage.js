import { Container } from "../components/styled/Container";

export const CharacterPage = () => {
    return (
        <Container>
            <h1>Page with Character Info</h1>
            <p>Page with a detailed info about a character.</p>
        </Container>
    )
}