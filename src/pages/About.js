import { Container } from "../components/styled/Container";
import { AboutHeader } from "../components/AboutHeader"

export const About = ( ) => {
    return (
        <Container>
            <AboutHeader/>
        </Container>
    )
}